"use client";

import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Sparkles, Brain } from "lucide-react";

interface AIsummaryProps {
  summary: string;
}

export function AIsummary({ summary }: AIsummaryProps) {
  return (
    <Card className="bg-slate-900/30 border-slate-800 rounded-3xl backdrop-blur-sm overflow-hidden">
      <CardHeader className="bg-emerald-500/5 border-b border-emerald-500/10">
        <div className="flex items-center gap-2">
          <Brain className="h-5 w-5 text-emerald-400" />
          <CardTitle className="text-lg text-white font-bold">Executive Summary</CardTitle>
        </div>
      </CardHeader>
      <CardContent className="p-8 relative">
        <Sparkles className="absolute top-4 right-4 h-6 w-6 text-emerald-500/20" />
        <p className="text-slate-300 leading-relaxed font-medium italic">
          &quot;{summary}&quot;
        </p>
        <div className="mt-6 flex items-center gap-3">
          <div className="flex -space-x-2">
            {[1, 2, 3].map((i) => (
              <div key={i} className="w-6 h-6 rounded-full border-2 border-slate-950 bg-slate-800 flex items-center justify-center text-[10px] font-bold text-slate-500">
                AI
              </div>
            ))}
          </div>
          <span className="text-[10px] uppercase tracking-widest font-black text-slate-500">Generated by Claude 3.5 Haiku</span>
        </div>
      </CardContent>
    </Card>
  );
}
